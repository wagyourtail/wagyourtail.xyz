import { PageListener } from "../../../../sitepage";
import { Express } from "express";

export const page = new PageListener("CS455 - AI", "Projects from the class I took on Artificial Intelligence. (in Unity).");